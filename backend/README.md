# GreenGrow Backend (Laravel 10)

Requirements:
- PHP 8.1+
- Composer
- MySQL (or other supported DB)

Setup:

1. Clone / create project
   ```bash
   composer create-project laravel/laravel backend "10.*"
   cd backend
